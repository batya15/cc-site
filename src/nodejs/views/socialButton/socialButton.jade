.v-socialButton
    .share
        != share
    span Контроль Холода в социальных сетях:
    each item in menu
        a.item(href=item.url, title= item.title, target="_blank", style="background-image: url(" + item.image + ")")
